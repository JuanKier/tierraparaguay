services:
  - type: web
    name: tierrapy
    env: node
    buildCommand: npm install && cd server && npm install && cd .. && npm run build
    startCommand: cd server && node index.js
    plan: free
    envVars:
      - key: TURSO_DB_URL
        sync: false
      - key: TURSO_DB_TOKEN
        sync: false
